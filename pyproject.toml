[project]
name = "scout"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "lmstudio>=1.5.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
